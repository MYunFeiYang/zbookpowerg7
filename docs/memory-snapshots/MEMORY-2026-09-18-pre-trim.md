# zbookpowerg7 黑苹果（HP ZBook Power G7 / OC 1.0.8 / MacBookPro16,4 / macOS 26.6.2）
> **铁律 + 索引**。细节 → `docs/memory-archive-2026-09.md`、`docs/sleep-tests/`、`.workbuddy/memory/<日期>.md`。

## 铁律
- **EFI 真源 = 工作区 `EFI/`（git）；ESP 只读**，FreeFileSync 镜像 `EFI/oc`→ESP。⚠️ `EFI/boot`、`EFI/scripts/` 不同步；自动触发会滞后 → 改完**手动点「开始」**，两边 `shasum -a 256` 一致才重启。
- 凡"不可行/已生效/封板"**必须实测或社区查证**并标来源；**严禁代理指标当判据**。
- `.workbuddy/` 被 gitignore（删了不可逆）；改 SSDT 前确认 DSDT 无同名对象。
- 🚫 别用 `PlistBuddy` 改 plist（`plistlib` + `sort_keys=False` + `plutil -lint`）；🚫 同一文件别并行多个 Edit；`sudo` 无免密 → `osascript … with administrator privileges`。

## 配置要点
- boot-args：`-igfxblt -igfxhdmidivs igfxonln=1 igfxrpsc=1 -wegnoegpu -amfipassbeta -lilubetaall alctcsel=1 revpatch=sbvmm rtcfx_exclude=80-FF -rtcfxdbg`
- `AppleXcpmCfgLock`=true｜`csr-active-config=0x0FFF`｜`ProcessorType=1793`｜`ScanPolicy` 不可限定｜**OTA 四件套** = RestrictEvents 1.1.7 + `revpatch=sbvmm` + Skip Board ID + `SecureBootModel=Disabled`
- 磁盘 s1 ESP / s3 NTFS(Win) / s4 APFS / s5 exFAT；网卡 AX201（CNVi，`Pci(0x14,0x3)`）；SSD = WD SN570 1TB TLC，位于 `Pci(0x1B,0x0)`＝RP17。

## ★ 睡眠 / 电源 → `docs/sleep-tests/`（README 横幅＝索引，**最新 §六十五**）
- 🛑 **三档终局**：S4 真休眠＝❌（⚠️ **21:3x 修正**：原写"6 次全失败"**作废** —— **干净样本只有 18:09/19:29 两次**：`HibernationFixup` 09-16 **10:42** 装入、message 明写需重启，而首次失败 **11:16** 距装入仅 34 min ⇒ 前三次桥状态不可证；**14:39 那次是 `standby 0` 致 `hibernatemode 25` 空转的无效测试**。3 次丢 RTC 触发 HP POST 005）；**S3＝❌ 实测不可用、已回滚收手**；**Deep Idle(S0ix)＝✅ 唯一可用**（唤醒 2.4 s，12.6 h 连睡无异常）。出远门直接关机。现役 **`hibernatemode 0` + `standby 0`**；`pmset-hibernate.sh` 已加硬闸（需 `FORCE_HIBERNATE=1`）。
- ★★ **S4「暂时 vs 没法」定论（§六十五）**：30 条准入条件里 **真正"没法满足"只有 1 条** = `#28` RTC 在 S4 断电期保持有效；**其余 9 条全是"暂时"**（4 条纯开关＝`HibernationFixup` 加回 / `hibernatemode` / `standby` / 内存压力；2 条只是**没去判**＝BIOS UEFI-only+CSM、`MmioWhitelist`；1 条**我判错**＝"镜像写不出来"（在 `hibernatemode 0` 下看不到 sleepimage 是**预期行为**，恒等于零证据）；1 条未测＝EC 在 S4 进出；1 条＝失败点在**恢复侧**而非"固件没实现 S4"——**断电动作实测已发生**）。
  - **且 `#28` 名义上还有 HP 官方解法，但已被本机读数基本关掉**：HP 支持文档 `ish_2843606-2359609-16` 原文 **「退出休眠状态后，系统时钟显示的时间不正确。……更新 BIOS 应该可能会解决该问题。」**，然 **本机 BIOS = `01.24.02 / 2026-05-11`（见下条，已实测读到），比 HP 2026-03 安全公告的该机型基线 `01.22.00`（SP161800）还新、且固件已含 S4 照旧失败** ⇒ **"固件老旧缺陷"假设在本机基本不成立**。⚠️ 未 100% 判死：HP 驱动页反爬、**2026-06 之后的列表没拿到** ⇒ "再核官网当前版本"仍是未结清动作。⚠️ **刷固件不可回退**；⚠️ **别与"BIOS 菜单无 AOAC 开关"混淆**（那指关 `Low Power S0 Idle`，是另一件事、另一条路）。
- 🏁 **S4 最终结论（2026-09-18 08:5x 给出，用户问「结论呢」）＝ 不追**。三条腿全断：**先例**（同机型报告只到 "Sleep"＝我们已有的 Deep Idle；同代 Fury G7 README 无 hibernation；AOAC 家族 0 先例；全球仅 4 台跑通 S4 —— T530/Q958/X250/Yoga Duet 7-13IML05，**全为 Legacy S3 世代**，越近现代越是"还在排错"）｜**配方**（唯一公开配方 T530 三件套对本机 **0/3 适用**）｜**机制**（真·没法满足仅 `#28` 一条，落在固件手里，而唯一开口"刷 BIOS"已关）。⇒ **剩余唯一未做实测 = B-1（`hibernatemode 3` 验写镜像），期望值判负**：通过也只证"镜像能写"、后面仍撞 `#28` 固件墙；不通过只把"未验证"改写成"实测否定" —— **两条都不改变结论，却要再赌一次 RTC 写入/HP POST 005** ⇒ 除非要"证据闭合"标签，否则不做。**收手态＝现状零改动**（`hibernatemode 0` + `standby 0`）。
- ★★ **本机真实 BIOS（2026-09-17 21:3x 实测读到）**：**`T75 Ver. 01.24.02`，发布日期 `2026-05-11`** —— ⏹️ **旧记录里的 `S71` 是错的，平台码 = `T75`**。机型 `HP ZBook Power G7 Mobile Workstation`、SKU `10J83AV`、EC/KBC 固件 `KBC Version 34.31.00`、固件厂商 `HP`、ABI `EFI64`。
  - **读法**：挂 Windows 分区（`diskutil mount disk0s3`，NTFS 只读）→ 读 `Windows/System32/config/**system**`（⚠️ **hive 名是小写**）里的 `\ControlSet001\Control\SystemInformation`（`BIOSVersion` / `FirmwareReleaseDate` / `OEMModelNumber` / `OEMModelSKU` / `OEMModelBaseBoardVersion`）；锚点串 `T75 Ver.`（UTF-16LE），⚠️ **可能奇字节对齐**，先找锚点再定偏移。
  - **已证伪的四条路**：macOS `system_profiler`/`ioreg` 的 SMBIOS（OC `UpdateSMBIOSMode=Custom` 换成 **Acidanthera 默认值**）｜`nvram -p` 找 HP/Setup 变量（**只有 10 键、零个 HP/Setup**）｜ACPI 表头（DSDT `OEMID=87EC` **正是 OC `NormalizeHeaders` 签名**）｜从 ESP `Firmware.BIN` 找明文版本号。
  - **唯一能读 "BIOS 设置项"（如 CSM 是否开）的路 = UEFI Shell `dmpstore -all`**；或用 **OC `ExposeSensitiveData` 2→6** 把真实 BIOS 信息暴露进 NVRAM（改 1 个数字 + 重启即可在 macOS 里 `nvram` 读）。
  - **⇒ 对 S4 的影响**：本机 BIOS（`01.24.02`/2026-05-11）比 HP 2026-03 安全公告里的 ZBook Power G7 **Minimum Version `01.22.00`（SP161800）** 还新，而 S4 照旧失败 ⇒ **"刷 BIOS 修 RTC 缺陷"期望值大幅下降**（未 100% 判死，HP 驱动页反爬、未拿到 2026-06 后列表）。引导侧三条独立证据（`FirmwareType 2` + `Is UEFI` + **`Not a PCAT machine`**）⇒ **UEFI-only 极可能已满足**。完整见 `docs/sleep-tests/bios-facts.md`。
- ★★ **判据纪律（已入 skill §0a-3）**：① **判"能力不满足"前先问「这个观测在当前档位下本来就是预期行为吗？」** —— 拿"配置关着时的正常状态"去证明"能力缺失"是无效的；要找「**配置已开 + 观测仍无**」。② **引用"N 次实测全失败"前，用 `git log -S "<键>" -- <配置文件>` 对齐每次测试的配置是否齐备** —— 缺桥/缺计时器/档位未生效的那几次是**无效测试，必须从 N 里剔掉**。
- **S3 支持性三层（可分开成立）**：L1 声明＝✅（`_S3`@`DSDT.dsl:38257`、`SS3` 是常量 `One`@5706、FADT `FLAGS=0x002384A5` 里 `HW_REDUCED_ACPI=0`+`RTC_S4=1` 与 bit21 AOAC 并存）｜L2 会选＝✅（关 `SSDT-DeepIdle` 后 `IOPMDeepIdleSupported` **属性整个消失**、`sleep states` 去 S0、`lastSleepType 0x02`）｜**L3 不能用＝❌**。
- ★★★ **L3 铁证（同机 A/B）**：`WakeTime` 2.4 s → **159.336 s（65×）**；`ApplePS2Controller SetState 2` 460 ms → **157,735 ms（342×）**；**全机唯一 panic**（`Kernel-2026-09-17-114214.panic`，NMIPI/TLB timeout，落 `IOUSBHostFamily`/`IntelBluetoothFirmware`）⇒ 全机 panic 总数 = 1。
- 🔍 **根因＝EC 在 S3 恢复后不响应**：`AppleACPIEC EC OBF=1 poll timed out` 每 ~2.2 s 连绵（Deep Idle 窗口 0/0｜S3 两次 34/120｜回滚后 0）⇒ PS2/KBC 与 VirtualSMC 的 SMBus **都经 EC** ⇒ 死等 160 s ⇒ USB/蓝牙/摄像头重枚举失败 ⇒ panic 落 USB 栈。**机制排除**：RTC 防护只碰 I/O `0x70/0x71`，EC 在 `0x62/0x66`，**端口不相交**（4 项 RTC 防护保留且必需）。
- 📚 **社区（OC-Little《01-关于AOAC》）**：原文 *"AOAC 和 S3 本身相矛盾…一旦进入 S3 就睡眠失败…只能强制关机"* + *"禁止 S3 可解决"* + *"未压降 5–10%/h"* ⇒ 用户症状逐字命中。`icelake-hackintosh` 给的三条路（daliansky SSDT 补丁 / BIOS 关 AOAC / SSDT+rename）**本机 HP 全不适用**。
- 🚀 **Deep Idle 压功耗**：父页「AOAC解决方案」7 条**逐条实测后已基本用尽** —— ✅禁 S3｜✅独显断电（`SSDT-dGPU-PowerOff-Darwin.aml` 调 `PEGP._OFF` 断电；`-wegnoegpu` 仅屏蔽驱动，独显硬件仍在，PEGP@0 在 ACPI 命名空间真实在线）｜✅**电源空闲管理＝`SSDT-DeepIdle` 本身**（`01-3` 目录唯一文件即 SSDT-DeepIdle.dsl）｜✅SSD 已是 TLC｜⚠️SSD 固件 `234100WD`（官方工具仅 Windows，建议不做）｜✅NVMeFix 1.1.4 已加载（`"NVMeFix"=1`）｜⚠️ASPM：**26 条 `pci-aspm-default=3` 注入已于 2026-09-17 整批移除**（commit `ebf6d5c`）；逐条实测**23/26 天生无效**（15 条注在 ASPM 字段=0 的 PCH 内建/私有链路设备，8 条注在不存在的设备 `1C` 深链/`1D`）⇒ **真受影响仅 3 条**（`PEG0`/`RP17`/`pci-bridge@1C`）；**全机能做 ASPM 的 PCIe 链路只有 4 条**（+`PXSX`=WD SN570），**SSD 本体从未被注入**（其省电走 NVMeFix APST）。⚠️别用「XHC 带 `enable-l1-aspm`」反驳"全禁用"（XHC 无 PCIe 链路）。详见 `docs/sleep-tests/aspm-audit.md`。
- **功耗地板**：Deep Idle ≈5 W ≈7%/h（正落在社区"未压降"5–10%/h 区间 ⇒ 本机就是未压降基线）；社区压降案例 0.66%/h 是**别人机器**，HP 无先例。**唯一最大杠杆＝睡眠关 Wi-Fi/BT —— 用户已明确否决（"别动WiFi蓝牙"）⇒ 剩余全是边角。**
- **pmset 实测已最省态**：`powernap 0`／`tcpkeepalive 0`／`womp 0`／`proximitywake 0`／`standby 0`／`hibernatemode 0`／电池 `lowpowermode 1`；**09-16 20:13→09-17 08:52 连睡 12.6 h 中间唤醒 0 次** ⇒ "清定时唤醒"收益≈0（AC 下 `sleep 0`）。
- ⚠️ **WorkBuddy 的 `NoIdleSleepAssertion`**（pid 671 Electron）：**只挡"空闲自动睡眠"**（电池场景），**不是合盖不睡的原因** —— §四十二 铁证：`11:29:11` 断言在场、`11:29:41` 机器**照样睡**（显式 `Software Sleep` 不受用户态断言约束）。USB 内核断言 3 条 = HP HD Camera / Bluetooth / USB Optical Mouse。
- ★ **合盖睡眠（§四十三，实证可用）**：**由 `Clamshell.app`（pid 2024，偏好 `whenClamshellIsClosed=sleep`）以显式 `Software Sleep` 发起，不经 idle 路径 ⇒ 不受 WorkBuddy `NoIdleSleepAssertion` 阻挡**（§四一「WorkBuddy 挡了」/ §四二「流程没启动·内核没感知」两归因均作废）。**15:53 实测一轮完整合盖 Deep Idle 睡眠**：`PMRD: clamshell closed 1, disabled 0/0, desktopMode 1, ac 1` ⇒ 内核**感知到合盖**且 `clamshellSleepDisabled=0`；`darkwakelinger` 链启动；`kIOMessageSystemWillSleep[134] to pid 2024 Clamshell`；`15:58:03 Wake from Deep Idle due to Lid Open`、`WakeTime 2.406s`、PS2 仅 458ms。**⇒ §四二对这次不成立**，12:14/13:18 属条件性偶发（Clamshell 时机问题，非机制故障）。要合盖即睡：**直接合盖即可**（Clamshell 接管）；保险用「苹果菜单→睡眠」。⛔ 别动 `SSDT-LID-G7`。
- 📋 **S4 准入条件全清单 → `docs/sleep-tests/s4-requirements-audit.md`（30 条 · 六层 · 逐条判定，2026-09-17）**：**18 满足 / 2 不适用 / 4 待验证 / 6 不满足** ⇒ **能凑约 90%，差的 10% 全在固件侧、改配置改不出来**（固件是否真实现 S4｜RTC 断电期能否保持｜镜像能否写出）。六层 = L0 固件声明 / L1 OpenCore / L2 NVRAM-RTC 协议 / L3 pmset / L4 存储内存 / L5 硬件结构。
  - ★ **更正（推翻"自锁"读法）**：**`rtcfx_exclude` 覆盖 `0x80–0xAB` 不是封杀** —— RTCMemoryFixup README 那句 *"hibernation won't work"* 只描述**无 `HibernationFixup` 的原生路径**；**有**它 + `HibernateMode=NVRAM` 就是**设计**（T530 原始线程 jozew321 原文：*"disabling the hibernation using the RTC memory that just causes a lot of trouble … NVRAM … way more stable than the old RTC stuff"*）。本机 `4ceea3a` message 当初也是这么写的。**现况 = 方式已配对、桥不在**（`HibernateMode=NVRAM`✅ `WriteFlash=true`✅ `AppleRtcRam=true`+`rtc-blacklist 0x0E–0x73` 不含休眠区✅ / **`HibernationFixup` 引用数 = 0 ❌**）。
  - **★ 3 条新查出的前置条件**：① **BIOS 须 UEFI-only + CSM disabled**（T530 原始线程明文，**本机从未核**，零风险 6 分钟）② `DevirtualiseMmio=true` 且 **`MmioWhitelist` 0 条** —— OC 官方明文把该 quirk 与 *"proper NVRAM and hibernation functionality"* 挂钩 ⇒ **待验证**（`Misc/Debug/Target=0` 无日志）③ `NVRAM/WriteFlash=true` 是 `HibernationFixup` 写 NVRAM 的前提（已具备）。
  - ⚠️ **路径坑**：**`sleepimage` / `swapfile*` 在独立 VM 卷 `/System/Volumes/VM`（APFS `disk1s6`, `noexec,nobrowse`），`/var/vm` 空是假象**（本机 `/var/vm` 空但 `vm.swapusage` 报 6144 M，实见 6 个 swapfile）⇒ 判"有没有写过休眠镜像"必须查 VM 卷。
  - **⇒ 下一步 = T1（BIOS，零风险）→ T2（`pmset -c hibernatemode 3` + `sleepnow` 睡 30 s，查 VM 卷 sleepimage 是否 ≥8 GiB，5 分钟定生死；不过 ⇒ S4 判死）→ 通了才谈 T3（加回 `HibernationFixup` + `mode 25`，中高风险）。** 回滚 `sudo pmset -c hibernatemode 0`。
- **待办**：✅ ① 已结清 —— 09-17 **13:03 重启**，`IOPMDeepIdleSupported=Yes` 已回来 ⇒ **Deep Idle 已恢复**、panic 总数仍 = 1、工作区=ESP（`f7261b16`）；⚠️ ② **「拔电池测」已撤回**（§三十九）：测了也不能优化（唤醒源已 0 次/12.6h，剩下是结构性漏电），插电场景下 5 W = 墙插功率 ≠ 掉电率 ⇒ **不必测**，评估出差续航时再说；⛔ ③ **ASPM/APST 不是机会而是已撞墙的路径** —— git 实证 `92af1f5`(2025-07 批量注入) → `e9e012e` → **`7b0ab03`(2026-07-08，1.5h 后回退)** → `04d3b60`(收敛到 `ps-max-latency-us`，message「提升睡醒稳定」) ⇒ **不动**；🔍 ④ 顺手排除：本机**无 Thunderbolt 控制器**（ioreg 零节点）⇒ TB 漏电嫌疑排除。
- ⚠️ **口径更正**："macOS 26 Tahoe 上 AirportItlwm 不工作"**已过期** —— 本机 `en1` 正常、`IO80211 Family 12.0`、`Wake On Wireless: Supported`。
- ★ **方法论**：**"固件声明了" + "OS 选了" ≠ "能用"** ⇒ 判 L3 只能实测，判据是"**醒得回来且醒得正常**"（同机 `WakeTime`/`Kernel Client Acks` 历史对照）。**"唤醒慢/失败"别停在"谁叫醒的"，要问"哪个驱动在等谁"**。判"是不是我改配置改坏的"：先列混淆变量（`last reboot` 对齐 `git log --format='%h|%ci|%s' -- EFI/OC/config.plist`），再用**端口/驱动栈是否相交**做机制排除。**AOAC 类补丁成对，关一个必须关配套件。**
- ★ **查证纪律**：改配置前按三层查 —— ① 同作者父/邻页（§二十八 只读子页 `01-4`、漏读父页 `01-关于AOAC` ⇒ 白吃两次实测 + 一次 panic）② 平台汇总仓库问题清单 ③ 症状原话搜论坛；三层无先例才实测，且实测前写清**预期形态 + 回滚点 + 不可逆风险**。来源分级：`github.com/*`／远景／tonymacx86 = 可当判据；技术博客／QuickSpecs = 仅方向；AI 内容农场 = 不可用。
- 📌 **变量台账**：整轮动过 6 个 = 4 项 RTC 防护（`rtcfx_exclude 0E-FF`／`AppleRtcRam true`／`rtc-blacklist 242B`／`NVRAM Delete`，**均保留**）+ 2 个 ACPI 开关（`SSDT-DeepIdle`／`SSDT-PCI0.LPCB-Wake-AOAC`，**均回滚 true**）⇒ **睡眠相关变量 = 0**。
- ⚠️ `SSDT-PCI0.LPCB-Wake-AOAC`（Comment `pair with DeepIdle`）从"元凶"**降级为次要贡献者**（Deep Idle 时代同样有 `LPCB XDCI` 唤醒却只要 2.4 s）⇒ **方案 B（GPRW 关 GPE 0x6D）作废**。
- ★ **USB 端口映射悬案已结**：本来就对。🔴 `USBPortType` ≠ `usb-port-type`；`com.apple.usb.externaldevice.*` 是**通用断言名**（内置蓝牙也用），不能据此判"误标外部"。
- ⚠️ FADT 字段偏移易错：`PM1a_EVT_BLK`@**0x38**（0x24 是 `FIRMWARE_CTRL`）、`GPE0_BLK`@**0x50**、`FLAGS`@**0x70(112)**。

## 工具坑
🚫 `log show` 沙箱直连硬禁 → 经 `osascript … with administrator privileges`；⚠️ **全窗口扫描被 SIGKILL(137)** ⇒ 必须 `--predicate` + 缩小窗口｜🚫 `dmesg` 读不到启动期（环缓冲 128 KB 被 IGPU 刷爆）｜★ **验证 kext 真生效 = `ioreg -d 0 -l -w0` 的 `IOKitDiagnostics→Classes` 实例计数**（别用 `ioreg -c 类名`：hook-only kext 不留节点，会给出相反结论）｜⚠️ **`ioreg -p IOACPIPlane` / `-p IOPCIDevice` 都只返回 0 行**（非有效 plane；路不通 ≠ 对象不存在）⇒ 查 PCI 树用 **`-c IOPCIDevice -t`**；**PCI 地址 → ACPI 名用 `"acpi-path"` 属性反查**（如 `RP17@1b0000` ⇒ `Pci(0x1B,0x0)`＝SSD），**不需要 Hackintool**｜`system_profiler SPPCIDataType` 本机返回空，不可作判据｜⚠️ Lilu 系 RELEASE 构建把 `DBGLOG` 编译期删除 ⇒ `-xxdbg` 惰性｜⚠️ BSD grep 不支持 `\s`/`\|`/`\b` → 一律 `grep -E`｜`iasl -d` 覆盖同名 `.dsl`｜zsh glob 无匹配会中断｜⚠️ `log show --start/--end` 查旧窗口不可靠 ⇒ 旧窗口结论一律降级、须复核时间戳。

## 状态
全功能机，已到 diminishing returns。妥协：USB-C 仅 USB2／更新只能全量／SMCBattery 自旋锁／IGPU #967／beta boot-args／SIP 全关／电池 77%（70.6 Wh）。**睡眠＝Deep Idle（~5 W，唤醒 2.4 s）**；S3、S4 均已实测判死。出远门直接关机。**ACPI 表现 14 张**（§45 误删 `SSDT-dGPU-PowerOff-Darwin` 已撤销恢复：本机确有独显，靠该 SSDT 调 `PEGP._OFF` 断电；其余 5 张删除＝TPD3x2/TB3HPx2/OCLT-S3Fix 均原 Disabled 无副作用）—— 见睡眠测试 §四十五/§四十八。
